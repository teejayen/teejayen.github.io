name: Scheduled Rebuild

on:
  schedule:
    - cron: '0 14 * * *'  # Daily at midnight AEST (14:00 UTC)
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cloudflare Pages rebuild
        run: curl -X POST "${{ secrets.CLOUDFLARE_DEPLOY_HOOK_URL }}"
